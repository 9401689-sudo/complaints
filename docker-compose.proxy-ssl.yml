services:
  complaints-proxy:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/complaints-proxy-ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/certs:/etc/nginx/certs:ro
